app = "g7-news-cron"
primary_region = "nrt"  # Tokyo — minimizes latency to NHK/Japanese RSS

[build]
  dockerfile = "Dockerfile"

# Environment variables — set secrets via: fly secrets set KEY=value
[env]
  TZ = "Asia/Tokyo"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
